import { GraphNodeCheckList } from '../../interfaces/auxiliary/type-checklist';
import { GraphNodeTodo } from '../../interfaces/auxiliary/type-checklist';
export const initTodoListComponent: GraphNodeCheckList = {
	todoItems: [],
};

export const initTodoComponent: GraphNodeTodo = {
	todoItem: undefined,
};

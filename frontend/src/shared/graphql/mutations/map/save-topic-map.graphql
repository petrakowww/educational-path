mutation saveTopicMap($input: SaveTopicMapInput!) {
  saveTopicMap(input: $input) {
    id
    routeId
    updatedAt
    nodes {
      id
      title
      type
      meta
      zIndex
      checklist {
        id
        text
      }
    }
    edges {
      id
      sourceId
      targetId
      meta
    }
  }
}

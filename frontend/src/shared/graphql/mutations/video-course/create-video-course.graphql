mutation CreateVideoCourse($input: CreateVideoCourseInput!) {
  createVideoCourse(input: $input) {
    id
    title
    description
    imageUrl
    isPublished
    topicNodeId
    createdAt
    updatedAt
  }
}
mutation updateRoute($id: ID!, $data: UpdateRouteDto!) {
  updateRoute(id: $id, data: $data) {
    id
    title
    description
    privateType
    createdAt
    updatedAt
    tags {
      id
      tag {
        id
        name
      }
    }
    topicNodes {
      id
    }
  }
}

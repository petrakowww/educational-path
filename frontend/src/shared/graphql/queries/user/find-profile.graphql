query FindProfile {
	findProfile {
		id
		name
		avatar
		method
		isTwoFactorEnabled
		email
		accounts {
			type
		}
		skillProfile {
			githubUrl
			vkUrl
			telegramUrl
			headline
			profilename
		}
	}
}
